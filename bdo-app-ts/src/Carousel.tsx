import React, { useState, useEffect } from 'react';
import Table from './Table';

interface IProps {
    inputs: Inputs;
}

interface Inputs {
    type: string;
    baseC: string;
    preC: string;
    postC: string;
}

const Carousel: React.FC<IProps> = (props: IProps): JSX.Element => {

}
